/* ══════════════════════════════════════════════════════════════════════
   SUPABASE CONFIG - STUB LEGACY (registro-costos)
   Codigo movido a ../_shared/supabase-config.js como fuente unica.
   Este archivo queda como retrocompatibilidad por si algun import
   antiguo sigue apuntando a este path.
   ══════════════════════════════════════════════════════════════════════ */
window.__FTP_PING_TABLE = 'config_costos';
if (!window.__FTP_SHARED_LOADED) {
  document.write('<script src="../_shared/supabase-config.js"><\/script>');
  window.__FTP_SHARED_LOADED = true;
}
